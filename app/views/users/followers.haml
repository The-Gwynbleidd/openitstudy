.col-md-10.users
  %h2 Followers
  -@followers.each do |f|
    .col-md-3.user.thumbnail
      .user-avatar
        =image_tag(f.avatar.url(:square), class:"img img-responsive")
      .user-info
        =link_to f do
          =f.first_name
          =f.last_name
